#include <stdio.h>

void test(void)
{
	char str1[] = {"hello", "world", NULL}
	char *str2 = {"hello", "world", NULL}
	char *str3[] = {"hello", "world", NULL}
	char **str4 = {"Hello", "World", NULL}

	while (str1[i] != NULL)
	{
		printf("%s", str1);
		i++;
	}

